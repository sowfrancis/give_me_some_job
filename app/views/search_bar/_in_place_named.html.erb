<div class="container">
	<div class="row">
    <div class="search_bar col-md-6">
			<h2>Un job prés de chez toi?</h2>
				<%= form_for :search , url: jobs_path, html: { :method => "get", id: "search-form"} do |f| %>
          <div id="custom-search-input">
              <div class="input-group col-md-12">
                  <%= f.text_field_tag :search, params[:search], class: "form-control input-lg", id: "autocomplete",  placeholder: "Search jobs"%>
                  <span class="input-group-btn">
                    <%= button_tag(type: "submit", class: "btn btn-default") do %>
										  search 
										<% end %>
                  </span>
              </div>
          </div>
        <% end %>
    </div>
	</div>
</div>
<%= render "script_js/script_autocomplete" %>